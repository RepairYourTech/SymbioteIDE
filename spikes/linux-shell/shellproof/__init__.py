"""The shell proof driver's concerns, one per module.

The map from each concern to the module that owns it is the entry point's own
docstring in the driver beside this package, which is also what the command line
prints; it is stated there once, so a module added, moved or renamed is one edit and
this file cannot carry a second copy of it to go stale.
"""
