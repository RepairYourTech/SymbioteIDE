[package]
name = "symbiote-auth"
version = "0.1.0"
edition = "2021"
description = "Authentication system for Symbiote IDE"

[dependencies]
symbiote-core = { path = "../symbiote-core" }
tokio = { workspace = true }
serde = { workspace = true }
