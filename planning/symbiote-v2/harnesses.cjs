'use strict';

module.exports = [
  {
    key: 'E01', name: 'Claude Code', wave: 0, priority: 'P0', deps: ['D03'],
    instructions: 'Global and project CLAUDE.md discovery; modular .claude/rules; project shared .claude/settings.json; project-local .claude/settings.local.json; user settings under ~/.claude; verify managed/admin policy and CLAUDE_CONFIG_DIR behavior.',
    capabilities: '.claude/agents, .claude/skills, slash commands, hooks, plugins, MCP project/user/local scopes, permissions/sandbox, auto-memory, session state, settings and environment behavior must be separately mapped.',
    integration: 'Claude Agent SDK / Claude Code programmatic agent surface, with CLI only for installation/profile probing where necessary.',
    specific: [
      'Determine exactly which resources follow CLAUDE_CONFIG_DIR and which remain outside it; test multiple simultaneous profiles.',
      'Distinguish project-shared versus project-local settings and ensure Symbiote never commits machine-local credentials or permissions accidentally.',
      'Map .mcp.json and user/local/project MCP semantics, tool allow/deny/ask rules, hooks, plugins, custom subagents, skills, and memory loading precedence independently.',
      'Verify Claude Max/Pro login reuse versus API/provider authentication and what the Agent SDK officially supports for distribution.',
      'Test session resume, compaction, subagent visibility, approval callbacks, image/content support, structured tool registration, and token/usage visibility.',
    ],
    acceptance: ['Two roles using distinct CLAUDE_CONFIG_DIR profiles can carry different accounts/tools/rules without leakage.', 'Project-local permission decisions are not projected into shared Git state unless explicitly requested.'],
    refs: [
      'https://docs.anthropic.com/en/docs/claude-code/settings',
      'https://docs.anthropic.com/en/docs/claude-code/memory',
      'https://docs.anthropic.com/en/docs/claude-code/mcp',
      'https://docs.anthropic.com/en/docs/claude-code/sub-agents',
      'https://docs.anthropic.com/en/docs/claude-code/hooks',
      'https://docs.anthropic.com/en/docs/claude-code/sdk',
      'https://github.com/anthropics/claude-agent-sdk-typescript',
    ],
    adapter: {
      key: 'E02', wave: 1, priority: 'P0',
      specific: ['Expose Symbiote orchestration tools to the Lead/worker through the supported SDK tool mechanism.', 'Preserve Claude-specific content blocks, subagent/tool events, permission requests, and compaction/session metadata in the normalized event stream.'],
      acceptance: ['A Claude/Fable or Claude/Opus profile can serve as the project Lead without becoming the canonical task store.', 'The adapter resumes a real Claude Code session after desktop restart and can continue a task-owned worktree.'],
    },
  },
  {
    key: 'E03', name: 'OpenAI Codex', wave: 0, priority: 'P0', deps: ['D03'],
    instructions: 'Global $CODEX_HOME/config.toml (normally ~/.codex/config.toml), trusted project .codex/config.toml, AGENTS.md/AGENTS.override.md hierarchy from repository root toward CWD, and any administrator requirements must be modeled separately.',
    capabilities: 'Skills from officially supported locations such as .agents/skills and ~/.agents/skills; MCP in supported config scopes; app-server protocol, approvals/sandbox, model/provider/profile settings, rules/instructions, sessions, usage, plugins or evolving features must be verified from current official OpenAI documentation.',
    integration: 'Codex app-server protocol as the primary UI/agent integration; CLI/headless only where app-server lacks a documented capability.',
    specific: [
      'Map config keys that project scope may not override and the exact behavior of trusted versus untrusted repositories.',
      'Do not invent a .codex/skills convention if current Codex officially discovers .agents/skills or another location instead.',
      'Test AGENTS.override.md and nested AGENTS.md precedence, size limits, fallback filenames, and working-directory effects.',
      'Separate Codex authentication/config home from Symbiote GitHub credentials and evaluate multiple-account shadow/config-home strategies safely.',
      'Map app-server initialization, thread/session lifecycle, item events, approval/user-input requests, model changes, diffs, command execution, and protocol-version handling.',
    ],
    acceptance: ['The contract reproduces effective Codex instructions and config for nested multi-root workspaces.', 'Project config is ignored or constrained exactly as official trust rules require.'],
    refs: [
      'https://developers.openai.com/codex/config-reference',
      'https://developers.openai.com/codex/guides/agents-md',
      'https://developers.openai.com/codex/skills',
      'https://developers.openai.com/codex/mcp',
      'https://developers.openai.com/codex/app-server',
    ],
    adapter: {
      key: 'E04', wave: 1, priority: 'P0',
      specific: ['Implement full app-server request/notification lifecycle with protocol negotiation and preserve raw Codex item/event evidence.', 'Support profile/account isolation only through mechanisms validated by the contract; do not rewrite HOME globally.'],
      acceptance: ['A Codex profile can act as Lead or worker with real approvals, resumable thread identity, structured events, and task-owned worktrees.', 'Nested AGENTS/config behavior in adapter-launched tasks matches direct Codex behavior.'],
    },
  },
  {
    key: 'E05', name: 'Pi Coding Agent', wave: 0, priority: 'P0', deps: ['D03'],
    instructions: 'Global ~/.pi/agent settings/resources, project .pi/settings.json and .pi resource directories, AGENTS.md/CLAUDE.md context discovery, and officially supported shared .agents/skills behavior must be mapped independently.',
    capabilities: 'Pi skills, extensions, prompt templates, themes, packages, provider/model configuration, auth/session state, RPC mode and Node SDK; explicitly document capabilities intentionally absent from core (for example MCP/subagent/plan/permission systems) versus extension-provided behavior.',
    integration: 'Pi Node SDK where embedding is appropriate and Pi JSONL RPC mode for process-isolated Host integration.',
    specific: [
      'Resolve the current official repository/documentation location after any ownership/redirect transition and pin tested package versions.',
      'Map project `.pi/skills` plus officially supported `.agents/skills`, global equivalents, extension discovery, settings precedence, package resources, and context-file loading.',
      'Separate auth/session data under ~/.pi/agent from portable project resources and test multiple provider/model profiles.',
      'Do not advertise native MCP, subagents, plan mode, or permission primitives if they are extension-layer rather than core capabilities.',
      'Map RPC framing, commands, events, tool calls, extension UI requests, session resume, compaction, branching, and SDK lifecycle.',
    ],
    acceptance: ['The support matrix distinguishes core Pi from installed extension capabilities at runtime.', 'A role can use project `.pi` resources without leaking another role’s global extension/config state.'],
    refs: [
      'https://github.com/badlogic/pi-mono/tree/main/packages/coding-agent/docs',
      'https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/rpc.md',
      'https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/sdk.md',
      'Resolve any official repository redirect before implementation and update these starting references.',
    ],
    adapter: {
      key: 'E06', wave: 1, priority: 'P0',
      specific: ['Select SDK versus isolated RPC per Host/security deployment and make both pass one normalized lifecycle contract.', 'Surface extension-provided capabilities only after Pi reports/probes them; never infer from files alone.'],
      acceptance: ['Pi can run a GLM/local-model role on a headless Fedora Host through structured RPC.', 'Session continuation and provider/model identity survive client reconnect without transcript scraping.'],
    },
  },
  {
    key: 'E07', name: 'OpenCode', wave: 0, priority: 'P0', deps: ['D03'],
    instructions: 'Managed/remote configuration, global ~/.config/opencode/opencode.json, custom config, project opencode.json, project `.opencode/`, inline/environment configuration, AGENTS.md, and Claude-compatible fallbacks must be mapped in exact current precedence.',
    capabilities: '.opencode agents/commands/plugins/skills/tools/themes, shared .agents/skills and .claude/skills discovery, MCP, permissions, LSP, plugins, SDK/server, providers/models, sessions, and OPENCODE_CONFIG_DIR behavior.',
    integration: 'Official OpenCode SDK/server/programmatic API, with CLI used only for installation and capability probing.',
    specific: [
      'Test merge semantics among remote, global, custom, project, directory, and inline config rather than assuming replacement.',
      'Map plural resource directories and every compatibility discovery path with provenance so Symbiote can avoid duplicate projection.',
      'Determine whether one OpenCode server may safely serve multiple isolated project/role profiles and how OPENCODE_CONFIG_DIR affects state.',
      'Map agents versus primary session behavior, permissions, plugin code execution, MCP lifecycle, LSP configuration, and provider credentials.',
      'Test server session/event APIs, reconnect, share/export behavior, model selection, usage, and tool approval semantics.',
    ],
    acceptance: ['The contract explains exactly why any effective OpenCode setting/resource was loaded.', 'Compatible shared skills are referenced once without hiding role-specific divergence.'],
    refs: [
      'https://opencode.ai/docs/config',
      'https://opencode.ai/docs/rules',
      'https://opencode.ai/docs/skills',
      'https://opencode.ai/docs/agents',
      'https://opencode.ai/docs/commands',
      'https://opencode.ai/docs/mcp-servers',
      'https://opencode.ai/docs/permissions',
      'https://opencode.ai/docs/plugins',
      'https://opencode.ai/docs/sdk',
    ],
    adapter: {
      key: 'E08', wave: 1, priority: 'P0',
      specific: ['Use the official server/SDK event model and retain provider/model/tool identity across normalized turns.', 'Support managed OpenCode profile directories so project roles can differ without fighting global state.'],
      acceptance: ['OpenCode can serve as Lead or worker through structured APIs with approvals, resume, and accurate effective config.', 'Role-specific plugins/MCPs/permissions remain isolated and reversible.'],
    },
  },
  {
    key: 'E11', name: 'Gemini CLI', wave: 1, priority: 'P1', deps: ['D03'],
    instructions: 'User ~/.gemini/settings.json, project .gemini/settings.json, system defaults/overrides, environment/CLI precedence, GEMINI.md context, trust behavior, and any current migration to or coexistence with Google Antigravity tooling must be verified.',
    capabilities: 'MCP, hooks, extensions, custom commands, skills, custom agents, policies, sandbox/headless/ACP or IDE integration, authentication, sessions, models, telemetry, and trust controls.',
    integration: 'Official headless/ACP/IDE-integration surface if current and stable; otherwise structured CLI with explicit limitations.',
    specific: [
      'Distinguish Gemini CLI from Google Antigravity CLI/product and document migration/interoperability rather than conflating them.',
      'Map system-level policy override semantics that project configuration cannot bypass.',
      'Test project trust gates, GEMINI.md hierarchy/import behavior, extension discovery, hook execution, and policy files.',
      'Probe ACP/headless capabilities per installed version instead of hardcoding roadmap claims.',
    ],
    acceptance: ['The support verdict names the exact structured surface and tested version.', 'Managed project configuration cannot override system policy or leak untrusted-project settings.'],
    refs: ['https://github.com/google-gemini/gemini-cli/tree/main/docs', 'https://github.com/google-gemini/gemini-cli/tree/main/docs/cli', 'https://github.com/google-gemini/gemini-cli/tree/main/docs/hooks', 'https://github.com/google-gemini/gemini-cli/tree/main/docs/ide-integration'],
  },
  {
    key: 'E12', name: 'Cursor Agent CLI', wave: 1, priority: 'P1', deps: ['D03'],
    instructions: 'Project `.cursor/rules`, AGENTS.md, CLAUDE.md compatibility, editor/workspace rules and MCP state, user-level CLI/editor configuration, and trust boundaries must be documented separately.',
    capabilities: 'ACP, CLI/headless sessions, models, rules, MCP, skills/agents if current, hooks, permissions, editor-global state, auth/usage, and repository indexing/context behavior.',
    integration: 'Cursor Agent ACP as the preferred generic structured interface, with Cursor-specific contract overlays.',
    specific: [
      'Distinguish portable project resources from opaque editor-global state and do not claim Symbiote can safely rewrite unsupported internal databases.',
      'Map `.cursor/rules` metadata/globs/always-apply behavior and compatibility files independently.',
      'Determine whether CLI consumes editor MCP/config automatically and how to isolate two role profiles.',
      'Probe ACP session, approval, tool, diff, image, model, usage, and cancellation parity.',
    ],
    acceptance: ['Cursor can be classified as ACP-native only for capabilities actually exposed over ACP.', 'Import/bridge behavior for editor-owned settings is explicit and non-destructive.'],
    refs: ['https://cursor.com/docs/cli/acp', 'https://cursor.com/docs/cli/using', 'https://cursor.com/docs/context/rules'],
  },
  {
    key: 'E13', name: 'GitHub Copilot CLI', wave: 1, priority: 'P1', deps: ['D03'],
    instructions: 'Repository `.github/copilot-instructions.md`, path-scoped `.github/instructions/**/*.instructions.md`, officially supported AGENTS.md/CLAUDE.md/GEMINI.md compatibility, organization/repository policy, and CLI/user configuration must be mapped.',
    capabilities: 'Custom agents, skills, hooks, MCP, plugins, setup workflows, noninteractive mode, model selection, permissions, sessions, auth/entitlements, usage, and distinction from GitHub Coding Agent/IDE chat.',
    integration: 'Official structured/noninteractive Copilot CLI interface if sufficient; otherwise Deep or Generic support with GitHub-specific event/approval limitations.',
    specific: [
      'Keep Copilot harness authentication separate from Symbiote’s GitHub Issue/PR/Actions authority.',
      'Map path-scoped instruction matching and precedence across all compatible instruction formats.',
      'Distinguish local Copilot CLI, remote GitHub coding agent, and IDE integrations as separate execution products.',
      'Test whether session continuation and machine-readable output are stable enough for orchestration.',
    ],
    acceptance: ['Support claims never imply remote Coding Agent capabilities are present in the local CLI.', 'Instruction and organization policy precedence are reproducible.'],
    refs: ['https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot', 'https://docs.github.com/en/copilot/customizing-copilot/creating-custom-agents-for-copilot-coding-agent', 'https://docs.github.com/en/copilot/customizing-copilot/extending-copilot-coding-agent-with-the-model-context-protocol-mcp'],
  },
  {
    key: 'E14', name: 'Qwen Code', wave: 1, priority: 'P1', deps: ['D03'],
    instructions: 'User ~/.qwen/settings.json, project .qwen/settings.json, QWEN or compatibility context/instruction files, trust and environment/CLI precedence must be verified.',
    capabilities: 'MCP, skills, hooks, subagents, extensions, commands, permissions, worktrees, models/providers, ACP and evolving serve/HTTP/SSE surfaces.',
    integration: 'Official ACP or serve/programmatic surface when qualified; otherwise structured CLI.',
    specific: [
      'Map `.qwen/worktrees` or native worktree behavior and prevent conflict with task-owned Symbiote worktrees.',
      'Distinguish native resources from Gemini/Claude compatibility imports and record duplicate-prevention rules.',
      'Probe ACP versus HTTP/SSE/serve surfaces for lifecycle completeness and version stability.',
      'Test local/open-provider profiles and model/context metadata needed for routing.',
    ],
    acceptance: ['Worktree ownership is unambiguous and cannot create nested/colliding isolation.', 'The contract selects one primary structured surface with evidence.'],
    refs: ['https://qwenlm.github.io/qwen-code-docs/en/users/features/mcp/', 'https://github.com/QwenLM/qwen-code'],
  },
  {
    key: 'E15', name: 'Kiro CLI', wave: 1, priority: 'P1', deps: ['D03'],
    instructions: 'Project `.kiro/` steering/specs/hooks/agents/skills and user/global/admin configuration must be mapped with exact CLI-specific precedence.',
    capabilities: 'MCP, permissions, Powers/plugins, agents, hooks, skills, specs, steering, auth/models, sessions, headless/automation, trust, and distinction from Kiro IDE/Web/Mobile/Crew products.',
    integration: 'Official Kiro CLI headless/structured control surface if available; otherwise structured CLI/Generic support.',
    specific: [
      'Research Kiro CLI independently from Kiro IDE and cloud collaboration products.',
      'Determine whether `.kiro/specs` are native execution inputs, planning artifacts, or both, and how Symbiote artifacts should interoperate without duplication.',
      'Map Powers/plugin supply-chain and permission effects separately from MCP.',
      'Test hooks, steering, role-specific agents, and project trust behavior.',
    ],
    acceptance: ['The contract does not attribute IDE-only features to the CLI.', 'Kiro-native specs can be imported/referenced without becoming a second canonical Symbiote task system.'],
    refs: ['https://kiro.dev/docs/cli/chat/configuration/', 'https://kiro.dev/docs/cli/'],
  },
  {
    key: 'E16', name: 'Factory Droid', wave: 1, priority: 'P1', deps: ['D03'],
    instructions: 'Project `.factory/settings.json`, `.factory/settings.local.json`, legacy `.droid.yaml` migration, AGENTS.md/CLAUDE.md, custom droids, user/global configuration, and trust/permission scope must be mapped.',
    capabilities: '.factory/skills plus officially supported .agents/.agent skills, `.factory/droids`, hooks, MCP, plugins, permissions, headless/session APIs, models, accounts, usage, and migration behavior.',
    integration: 'Official Factory/Droid headless or session API if current; otherwise structured CLI.',
    specific: [
      'Test legacy `.droid.yaml` detection and migration without destroying hand-maintained content.',
      'Map settings-local Git exclusion expectations and precedence.',
      'Distinguish custom droids from Symbiote Team Roles and define import/bridge semantics.',
      'Map all skills discovery paths and avoid duplicating one skill across compatibility roots.',
    ],
    acceptance: ['Legacy and current projects reconcile safely.', 'Droid definitions do not become competing canonical role identities.'],
    refs: ['https://docs.factory.ai/', 'Use current official Factory configuration, skills, droids, hooks, MCP, and headless documentation.'],
  },
  {
    key: 'E17', name: 'Amp', wave: 2, priority: 'P1', deps: ['D03'],
    instructions: 'Project/user instructions, settings, permissions, skills, MCP, and any local versus Orb/cloud configuration must be mapped.',
    capabilities: 'TypeScript/Python SDK, streaming, thread continuation, tools, MCP, skills, permissions, code/web/Mac/iOS clients, auth/models/usage, and cloud data boundaries.',
    integration: 'Official Amp TypeScript or Python SDK where terms/capabilities permit; otherwise documented CLI/headless surface.',
    specific: [
      'Distinguish local execution state from Amp Orb/cloud thread state and document privacy/data transfer.',
      'Map SDK thread continuation and event semantics against Symbiote canonical sessions.',
      'Test MCP/skills/permissions projection and whether profiles can be isolated by role.',
      'Document what remains usable offline or without the Amp cloud service.',
    ],
    acceptance: ['Users can see precisely what project data leaves the Host.', 'Amp thread identity never substitutes for canonical task/artifact state.'],
    refs: ['https://ampcode.com/manual', 'Use the current official Amp SDK documentation.'],
  },
  {
    key: 'E18', name: 'Kimi Code CLI', wave: 2, priority: 'P1', deps: ['D03'],
    instructions: 'Native TOML/JSON configuration, project/user scopes, context/instruction behavior, environment/CLI overrides, and profile paths must be discovered from current official docs/code.',
    capabilities: 'Agent Skills, subagents, MCP, plugins, ACP, Wire mode, commands, permissions, auth/models/sessions/usage, and transport-specific feature differences.',
    integration: 'Official Wire mode or ACP when qualified; structured CLI fallback.',
    specific: [
      'Compare Wire mode and ACP capability matrices rather than assuming parity.',
      'Test known/evolving MCP behavior independently under each integration transport.',
      'Map plugin/subagent/skills native paths and profile isolation.',
      'Probe structured event, approval, continuation, cancellation, and usage data.',
    ],
    acceptance: ['The selected transport exposes every capability Symbiote advertises, or limitations are explicit per transport.', 'MCP is not advertised under a transport/version where it is unavailable.'],
    refs: ['https://github.com/MoonshotAI/kimi-cli', 'Use current official Kimi CLI ACP/Wire/configuration documentation.'],
  },
  {
    key: 'E19', name: 'Grok Build', wave: 2, priority: 'P1', deps: ['D03'],
    instructions: 'Verify official global/project config locations, GROK_HOME or equivalent profile behavior, instruction compatibility files, and project override limitations from current xAI sources.',
    capabilities: 'ACP/headless/structured control, plugins, MCP, skills, commands, permissions, sessions, models, auth/usage, worktrees, and CLAUDE/AGENTS compatibility claims.',
    integration: 'Official xAI structured/ACP surface if available and stable; otherwise structured CLI/Generic.',
    specific: [
      'Do not rely on community wrappers or unverified social posts for configuration paths.',
      'Probe exactly which project keys may override global configuration.',
      'Map plugin supply-chain and compatibility imports with provenance.',
      'Record product naming/version transitions explicitly.',
    ],
    acceptance: ['Every native-path and capability claim is backed by current official xAI evidence or a reproducible binary probe.', 'Unverified claims remain unsupported rather than guessed.'],
    refs: ['Use the current official xAI Grok Build/Grok CLI documentation and repository; reject similarly named community clients.'],
  },
  {
    key: 'E20', name: 'Mistral Vibe', wave: 2, priority: 'P1', deps: ['D03'],
    instructions: 'Project `./.vibe/config.toml`, user `~/.vibe/config.toml`, VIBE_HOME, admin/CLI/environment precedence, AGENTS.md, and trust behavior must be verified.',
    capabilities: '.vibe agents, skills, prompts, tools, logs, permissions, ACP, MCP if current, auth/models/sessions/usage, and configuration-home isolation.',
    integration: 'Official ACP when qualified; structured CLI fallback.',
    specific: [
      'Map user/project/admin precedence and which values are prohibited in project scope.',
      'Test `.vibe/agents/*.toml` and skills against Symbiote role/profile projection.',
      'Document logs/history portability and sensitive-content handling.',
      'Probe ACP lifecycle and permission parity.',
    ],
    acceptance: ['VIBE_HOME profiles isolate role/account configuration where officially supported.', 'Admin policy cannot be bypassed by project projection.'],
    refs: ['https://docs.mistral.ai/', 'Use current official Mistral Vibe/CLI documentation and repository.'],
  },
  {
    key: 'E21', name: 'Hermes Agent', wave: 2, priority: 'P1', deps: ['D03'],
    instructions: 'Global ~/.hermes/config.yaml, project-local `.hermes` behavior if current, AGENTS/context files, external skill directories, and environment/CLI precedence must be probed.',
    capabilities: 'MCP, plugins under ~/.hermes/plugins, skills including ~/.agents/skills, memory, sessions, tools, kanban/tasks, subagents, hooks if current, local/remote models, and headless operation.',
    integration: 'Official Hermes CLI/programmatic interface or structured CLI; evaluate its orchestration/kanban features as subordinate runtime state.',
    specific: [
      'Separate stable, experimental, and absent project-local/hook behavior by tested version.',
      'Ensure Hermes memory/kanban/subagent state does not compete with Symbiote canonical memory/task systems.',
      'Map local-model provider endpoints and Host placement requirements for the Strix Halo use case.',
      'Test external shared skill directories and duplicate discovery.',
    ],
    acceptance: ['Local-model execution can be profiled without leaking global Hermes memory/tasks into another Project.', 'Experimental features are capability-gated.'],
    refs: ['https://github.com/NousResearch/hermes-agent', 'Use current official Hermes Agent documentation.'],
  },
  {
    key: 'E22', name: 'Goose', wave: 2, priority: 'P2', deps: ['D03'],
    instructions: 'Discover exact current Block Goose user/project configuration, recipes/instructions, extensions, profiles, and trust scopes from official docs/repository; do not invent a `.goose/` convention by analogy.',
    capabilities: 'Extensions/MCP, recipes, providers/models, sessions, permissions, headless/server APIs, skills/agents if current, auth/usage, and desktop versus CLI differences.',
    integration: 'Official Goose programmatic/headless/server surface if available; generic structured CLI otherwise.',
    specific: ['Document desktop-only versus CLI capabilities separately.', 'Determine how recipes/extensions map to Symbiote commands, skills, or MCP without flattening distinct semantics.', 'Probe profile isolation and session continuation.'],
    acceptance: ['Every path is verified from current official evidence.', 'Support tier reflects the deepest proven interface, not Goose popularity.'],
    refs: ['https://github.com/block/goose', 'https://block.github.io/goose/'],
  },
  {
    key: 'E23', name: 'Aider', wave: 2, priority: 'P2', deps: ['D03'],
    instructions: 'Map `.aider.conf.yml`, user/project/environment/CLI precedence, conventions/read files, model settings/metadata, Git behavior, lint/test commands, modes, and chat-history state.',
    capabilities: 'Headless/CLI output, model/provider profiles, Git commits, lint/test integration, architect/editor modes, repository map, voice/image if current, sessions/history, and absence/presence of native MCP/skills/hooks.',
    integration: 'Structured CLI if stable machine-readable output exists; otherwise Generic PTY with explicit limitations.',
    specific: ['Do not invent native MCP, skills, subagents, or hooks if Aider does not provide them.', 'Prevent Aider’s automatic Git behavior from bypassing task-owned branch/worktree and commit policy.', 'Map configuration precedence and per-role profile isolation.', 'Document repo-map behavior as harness context, not a substitute for the System Graph.'],
    acceptance: ['Git actions are policy-contained.', 'Generic support limitations are clear if no structured session protocol exists.'],
    refs: ['https://aider.chat/docs/', 'https://github.com/Aider-AI/aider'],
  },
  {
    key: 'E24', name: 'Kilo Code', wave: 2, priority: 'P2', deps: ['D03'],
    instructions: 'Research Kilo Code CLI/desktop/extension configuration separately, including rules/instructions, modes/agents, MCP, skills, hooks, provider settings, and project/user scopes.',
    capabilities: 'CLI or ACP/headless surface, sessions, permissions, models/providers, worktrees, MCP, custom modes, skills, hooks, usage, and distinction from editor-extension state.',
    integration: 'Deep structured adapter only if Kilo exposes a supported standalone control surface; otherwise Import/Bridge or Generic CLI.',
    specific: ['Do not assume Roo/Cline ancestry makes paths or semantics identical.', 'Distinguish Kilo desktop/extension/CLI products.', 'Evaluate safe import of existing Kilo modes/rules without editing opaque IDE state.'],
    acceptance: ['The verdict is independent of Roo/Cline findings.', 'Opaque extension state is never rewritten without an official contract.'],
    refs: ['https://github.com/Kilo-Org/kilocode', 'Use current official Kilo Code documentation.'],
  },
  {
    key: 'E25', name: 'Google Antigravity CLI', wave: 2, priority: 'P2', deps: ['D03', 'E11'],
    instructions: 'Research the current official Google Antigravity product/CLI configuration, migration relationship to Gemini CLI, rules/skills/agents/hooks/MCP, scopes, trust, and profile behavior from Google sources.',
    capabilities: 'Structured protocol/headless/ACP, models/auth/usage, sessions, tools, permissions, extensions, remote/cloud features, and compatibility with Gemini CLI resources.',
    integration: 'Official Google structured surface if publicly supported; otherwise defer or Generic support.',
    specific: ['Keep this completely distinct from the user’s CFSA Antigravity repository/methodology.', 'Verify product existence, naming, release status, and migration claims as of implementation time.', 'Define coexistence/migration strategy with Gemini CLI without duplicate or destructive projection.'],
    acceptance: ['No CFSA artifacts or assumptions are attributed to Google Antigravity.', 'Support waits for a verifiable public integration/configuration contract.'],
    refs: ['Use current official Google Antigravity/Gemini CLI announcements, documentation, and repositories only.'],
  },
  {
    key: 'E26', name: 'Crush', wave: 2, priority: 'P2', deps: ['D03'],
    instructions: 'Discover Charmbracelet Crush global/project configuration, context files, providers/models, permissions, commands, MCP/skills if present, and scope precedence from official sources.',
    capabilities: 'CLI/headless/session protocol, LSP/tools, models/providers, MCP, permissions, logging/history, themes, and configuration/profile isolation.',
    integration: 'Official structured/headless interface if available; otherwise Generic PTY.',
    specific: ['Distinguish polished terminal UI capability from programmatic orchestration capability.', 'Map Bubble Tea/TUI process behavior for reliable cancellation/resume.', 'Do not infer MCP/skills from similarly positioned tools.'],
    acceptance: ['The support tier reflects machine-control reliability, not only interactive usability.', 'PTY completion/status confidence is explicit if required.'],
    refs: ['https://github.com/charmbracelet/crush'],
  },
  {
    key: 'E27', name: 'Roo Code', wave: 2, priority: 'P2', deps: ['D03'],
    instructions: 'Research Roo Code extension settings, modes, rules, commands, MCP, skills, browser/tools, checkpoints/tasks, provider profiles, and workspace/global storage with explicit distinction between file-backed and opaque VS Code state.',
    capabilities: 'Any official CLI/headless/A2A/control API, extension command API, export/import formats, sessions, approvals, usage, and migration potential.',
    integration: 'Import/Bridge unless a supported standalone structured control surface is proven.',
    specific: ['Do not automate or edit opaque VS Code globalStorage databases by reverse engineering.', 'Identify which project rules/modes/MCP configurations can be safely imported or projected.', 'Treat Roo’s internal task/session state as subordinate imported history, never canonical Symbiote state.', 'Document one-way versus round-trip bridge limitations.'],
    acceptance: ['The issue concludes with a precise bridge boundary and data-loss risks.', 'No “support” claim requires launching VS Code and scraping UI.'],
    refs: ['https://github.com/RooCodeInc/Roo-Code', 'https://docs.roocode.com/'],
  },
  {
    key: 'E28', name: 'Cline', wave: 2, priority: 'P2', deps: ['D03'],
    instructions: 'Research Cline extension/CLI product boundaries, rules/instructions, MCP, skills, hooks/workflows if current, provider profiles, workspace/global storage, checkpoints/tasks, and export/import surfaces.',
    capabilities: 'Official CLI/headless/control API if any, extension command surface, sessions, approvals, tools, usage, browser, and distinction between file-backed and opaque editor state.',
    integration: 'Import/Bridge or Generic support unless an official standalone structured interface is proven.',
    specific: ['Do not assume Roo-derived paths/behavior are interchangeable.', 'Never mutate opaque VS Code state without an official supported API.', 'Map safe import/export of Cline rules/MCP/settings and role isolation constraints.', 'Document whether Cline CLI and extension are the same runtime or separate products.'],
    acceptance: ['The support verdict is based on current official Cline interfaces.', 'Bridge support cannot misrepresent editor-session control as native Symbiote orchestration.'],
    refs: ['https://github.com/cline/cline', 'https://docs.cline.bot/'],
  },
];
